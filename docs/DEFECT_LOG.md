# Defect Log

| ID | Steps to reproduce | Expected | Actual | Severity | Priority | Status |
|---|---|---|---|---|---|---|
| DEF-001 | Submit a telemetry value at a threshold boundary | Boundary is classified according to rule | Record observed result | High | High | New |
| DEF-002 | Login as VIEWER and submit a configuration change | Access denied | Record observed result | Critical | High | New |
| DEF-003 | Attempt an invalid mission transition | Transition rejected | Record observed result | High | High | New |
| DEF-004 | Run `mvn clean test`, then check which test classes actually executed | All automated tests, including the seven Selenium system-test classes, run and are reported on | `pom.xml` excluded the JUnit5 `system` tag from Surefire with no replacement execution, so every system test except `MissionsTests` was silently skipped on every build; `MissionsTests` (which was missing the `system` tag) did run, but failed on all 5 cases with `net::ERR_CONNECTION_REFUSED` because nothing in the pipeline started the application first. Chrome also ran with a visible window instead of headless. | Critical | High | Closed — fixed by adding a Maven Failsafe execution bound to `integration-test`, wiring Spring Boot's `start`/`stop` goals around it so the app is running before Selenium connects, tagging `MissionsTests` consistently, and switching Chrome to `--headless=new`. |
| DEF-005 | Run the Selenium suite and open `/login` | The sign-in button, which has no `type` attribute, is still located and clicked via a `button[type='submit']` locator | Every test that logs in through `LoginPage` (`LoginSystemTest`, `DashboardTests`, `ConfigurationTests`, `RoleBasedAccessTests`) timed out waiting for a clickable `button[type='submit']`, because `login.html`'s sign-in button never declared `type="submit"` (relying on the implicit HTML default, which real browsers honour but the test's attribute selector cannot see). Every other template in the app explicitly sets `type="submit"` on its buttons. | Critical | High | Closed — added the explicit `type="submit"` attribute to the sign-in button in `login.html`, consistent with every other form in the app. |
| DEF-006 | Log in, then click "Apply filters" on the Satellites page (or "Save" on Configuration) | The filter/save action on that page's own form runs | `SatellitesPage.applyFiltersButton` and `ConfigurationPage.saveButton` (and `SatellitesTests`'s own inline locators) selected `button[type='submit']` with no scope. Every authenticated page's topbar also renders a "Sign out" button with `type="submit"`, earlier in the DOM than the page's own form, so Selenium clicked "Sign out" instead — 3 Satellites tests failed with the browser unexpectedly landing back on the login page. | High | High | Closed — scoped both locators to `main form button[type='submit']` (the topbar lives in `<header>`, outside `<main>`), and rewrote `SatellitesTests` to drive the page through the existing `SatellitesPage` Page Object instead of ad-hoc locators. |
| DEF-007 | Create a mission, then update its status from the missions table | The status-update test detects the mission row exists and updates it | `MissionsTests.missionsPage_updatesMissionStatus` checked whether the "no missions" placeholder was showing by reading `row.getAttribute("class")` on the `<tr>` — but the `empty` CSS class is set on a `<td>` inside the row, not the row itself, so the check never worked and the test grabbed the placeholder row, then failed with `NoSuchElementException` looking for a status dropdown that isn't there. | Medium | Medium | Closed — the check now looks for a `.empty` descendant of the row instead of the row's own class attribute. |
| DEF-008 | Click a topbar link right after a previous navigation; visit an unmapped URL | Navigation completes; the invalid-URL test observes the app's real error handling | `NavigationTests.topbarNavigation_worksForAllPages` clicked topbar links immediately after `findElement` with no wait for them to be interactable, causing an intermittent failure to navigate to `/missions`. Separately, `invalidUrl_redirectsToErrorPage` hit an unmapped URL while not logged in; since that path isn't on the security allow-list, Spring Security redirected to `/login` before the app's own error handling ever ran, and even once authenticated the app forwards internally to the error view without changing the browser URL, so the original URL-based assertion could never pass. | Medium | Medium | Closed — added `elementToBeClickable` waits before clicking topbar links, logged in before hitting the invalid URL, and changed the assertion to check the rendered error page's title/content instead of the URL. |
| DEF-009 | Submit the "Save configuration" form as Admin with battery limits of 20V/100V | Save succeeds (values were only ever meant to exercise the happy path) | The app correctly rejects the request (`Battery limits must be within 0-15V`, per `ConfigurationValidationService`), but the test's own hand-picked values violated that rule, so it hit the error page instead of a successful save. | Low | Medium | Closed — changed the test's battery values to 5V/12V, which satisfy the app's own validated range. |
| DEF-010 | Load any satellite's detail page and check it loaded correctly | `SatelliteDetailPage.isSatelliteDetailLoaded()`/`getSatelliteName()`/`getSatelliteCode()` reflect the real page | These locators referenced CSS classes (`.satellite-detail`, `.detail-container`, `.satellite-name`, `.detail-title`) that do not exist anywhere in `satellite-detail.html`, so `isSatelliteDetailLoaded()` silently always returned `false` even when the page had loaded perfectly (confirmed via screenshot). | High | Medium | Closed — locators rewritten to match the real template (the lifecycle form's `action` attribute, the page-header `<h1>`, and the first KPI value). |
| DEF-011 | Log in as an Operator or Viewer and open a satellite's detail page | The "Configure telemetry" link only appears for roles that can actually use it (Admin/Engineer) | `satellite-detail.html` rendered the "Configure telemetry" link unconditionally for every logged-in role; only the destination endpoint enforced the Admin/Engineer restriction, so lower-privileged users saw a link that would 403 if clicked. | Medium | Medium | Closed — added `sec:authorize="hasAnyRole('ADMIN','ENGINEER')"` to the link, using the `thymeleaf-extras-springsecurity6` dependency that was already in the project but unused. |
| DEF-012 | A Viewer requests `/configuration/{id}` directly | Access is denied (403) and the test observes that correctly | The app correctly returns 403 Forbidden, but Spring forwards internally to the error view rather than redirecting, so the URL never changes; the test's URL-based assertion (`contains("login")`/`contains("error")`/`contains("dashboard")`) could never match. | Low | Medium | Closed — changed the assertion to check the rendered error page's title/content, matching the fix already applied to `NavigationTests`. |
| DEF-013 | Apply a status filter on the Satellites page | The results table reflects the newly applied filter before the test asserts on it | `filterByStatus`/`searchSatellites` waited for `.table-wrap` to be present — but that element is already on the page *before* the click, so the wait resolved immediately regardless of whether the page had actually reloaded with the new filter, producing a race. | Medium | Medium | Closed — both methods now wait for the actual filtered URL (`status=...` / `q=...`) before returning. |
| DEF-014 | Sign out via the topbar | The browser returns to the login page | `BasePage.logout()` looked for `a[href*='logout']`, but Sign Out is implemented as `<form action="/logout"><button type="submit">`, not a link — no such `<a>` element exists anywhere in the app, so every logout attempt timed out. | High | Medium | Closed — locator changed to `form[action='/logout'] button[type='submit']`. |
| DEF-015 | Transition a mission to an invalid status (e.g. DRAFT -> DRAFT) | The app rejects the transition with a friendly validation message, consistent with every other validated action | `MissionService.transition()` throws a custom `InvalidStateTransitionException` for a disallowed transition, but `GlobalExceptionHandler` only handled `IllegalArgumentException`/`IllegalStateException`/`SecurityException`, so this exception went uncaught and surfaced as a raw `500 Internal Server Error` instead of the app's normal error page. `MissionsTests.missionsPage_updatesMissionStatus` selected the dropdown's first option (`DRAFT`) blindly, which is the mission's own starting status — an invalid no-op transition — and hit exactly this gap. | High | High | Closed (app + test) — added `InvalidStateTransitionException` to `GlobalExceptionHandler`, and changed the test to select `SCHEDULED` (a real valid transition from `DRAFT`). |
| DEF-016 | Save a satellite's configuration successfully | The test confirms the save landed on the page the app actually redirects to | The backend correctly redirects to `/satellites/{id}` after a successful save (by design), but the test asserted the URL still contained `/configuration/`, which can never be true after a successful save — the assertion contradicted the app's own intended behaviour. `ConfigurationPage.saveConfiguration()`'s wait (`.card form` present) was also fragile, since the satellite detail page it redirects to has its own unrelated `<form>`. | Medium | Medium | Closed — assertion now checks for `/satellites/`, and the wait condition now waits for that same URL instead of a generic form locator. |
| DEF-017 | Click a satellite in the fleet table to open its detail page | Navigation reliably completes | `SatellitesPage.clickFirstSatellite()`/`DashboardPage.clickFirstSatellite()`/`clickViewFleet()`/`SatelliteDetailPage.clickConfigure()`/`clickBackToSatellites()` occasionally didn't navigate on the first click in this environment (plain server-rendered links, no JS on the page, so this looks like a headless-Chrome/chromedriver timing quirk rather than an app issue) — intermittent, environment-dependent. | Medium | Low | Mitigated — added `BasePage.clickAndWaitForUrl()`, which retries the click once if the URL hasn't changed within the wait, and applied it to all of the above. |
| DEF-018 | Click "View fleet" on the Dashboard, or "Reset" on the Satellites page | Navigation goes to `/satellites` via the intended button | `DashboardPage.viewFleetButton` and `SatellitesPage.resetButton` both used the unscoped locator `a[href='/satellites']`. The topbar nav fragment (`fragments.html`) also renders its own `<a href="/satellites">Satellites</a>` link, earlier in the DOM than either intended button, so Selenium's `findElement()` matched the topbar link instead — the same class of bug as DEF-006, just for a different pair of buttons. In CI this surfaced as `clickViewFleet()`/`resetFilters()` clicking successfully but the URL never changing to reflect the intended navigation. | High | High | Closed — scoped the locators to `.page-header a[href='/satellites']` and `.actions a[href='/satellites']` respectively, matching only the intended button on each page. |
| DEF-019 | Type into the Satellites page search box (`#q`) in headless Chrome on CI | The typed value appears in the field and the search submits with it | `BasePage.sendKeys()` only waited for the target field to be *visible* before calling `.clear()`/`.sendKeys()`, but never explicitly clicked it first to give it keyboard focus. On CI's headless Chrome this occasionally caused `sendKeys()` to silently type into an unfocused field, submitting an empty query — `satellitesPage_resetFilters`/`satellitesPage_emptyResults`/`satellitesPage_filtersBySearch` all failed this way, with the field's value staying `""` for the full wait timeout. Not reproducible locally on Windows Chrome, which focuses on `sendKeys()` regardless. | High | High | Closed — `sendKeys()` now explicitly calls `.click()` on the target element before clearing/typing, guaranteeing focus before input. |
| DEF-020 | Click a button/link in headless Chrome on CI, under load | The native `WebElement.click()` registers normally | On rare occasions in CI, a syntactically correct click on an `elementToBeClickable`-confirmed element did not register at all — a known class of quirk with certain elements in newer headless Chrome versions, distinct from DEF-017/DEF-018 (which were locator-targeting bugs, not click-registration bugs). | Low | Low | Mitigated — `BasePage.click()` now falls back to a JavaScript-triggered click (`arguments[0].click()`) if the native click throws a `WebDriverException`, without changing behaviour for the normal case. |
| DEF-021 | Run the full system-test suite in GitHub Actions (2-core shared runner) | The suite passes consistently, matching local results | Even with DEF-017–DEF-020 fixed, a small number of system tests still failed intermittently on CI — never the same test twice in a row — consistent with CPU contention on GitHub's shared runners rather than any remaining code defect. An earlier attempt to fix this by adding an internal retry loop to `clickAndWaitForUrl()`/`sendKeys()` (each retrying up to 3 times) combined badly with Maven Failsafe's own `rerunFailingTestsCount`, multiplying total attempts per failing test (up to ~4 outer x 3 inner) and increasing one CI run to 17+ minutes with *more* failures than before, by starving other tests of CPU while the retries ran. | Medium | Medium | Mitigated — removed the internal retry loops from `BasePage`, keeping single-attempt actions; `pom.xml`'s Failsafe `rerunFailingTestsCount` (set to `2`, i.e. up to 3 total attempts per test) is now the sole retry mechanism. A test that fails all 3 attempts is still reported as a real failure, not silently hidden. |

Update this file with actual defects found during execution. Do not invent defects for the final report.
