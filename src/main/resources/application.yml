spring:
  datasource:
    url: jdbc:h2:file:./data/mission-control;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  thymeleaf:
    cache: false
server:
  error:
    include-message: always
